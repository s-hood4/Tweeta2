<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.LoginActivity">


    <TextView
        android:id="@+id/tvFirstScreen"
        android:layout_width="297dp"
        android:layout_height="112dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="153dp"
        android:text="See what's happening in the world right now."
        android:textColor="#000000"
        android:textSize="30sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/firstScreenRegisterButton"
        android:layout_width="297dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="174dp"
        android:background="@drawable/round_button"
        android:fontFamily="sans-serif"
        android:text="Get started"
        android:onClick="loginToRest"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="24sp" />

    <ImageView
        android:id="@+id/ivFirstScreenIcon"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_alignStart="@+id/tvFirstScreen"
        android:layout_alignParentTop="true"
        android:layout_marginStart="-1dp"
        android:layout_marginTop="27dp"
        tools:srcCompat="@tools:sample/backgrounds/scenic"
        android:src="@drawable/twitter_png"/>

    <TextView
        android:id="@+id/log_in_string"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/ivFirstScreenIcon"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="54dp"
        android:text="Have an account already?"
        android:textColor="#000000"
        android:textSize="16sp" />

    <Button
        android:id="@+id/firstScreenLoginButton"
        android:layout_width="52dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="92dp"
        android:layout_marginBottom="52dp"
        android:layout_marginLeft="5dp"
        android:onClick="loginToRest"
        android:background="@android:color/transparent"
        android:fontFamily="sans-serif"
        android:text="Log in"
        android:textAllCaps="false"
        android:textColor="#00aced"
        android:textSize="16sp" />
</RelativeLayout>